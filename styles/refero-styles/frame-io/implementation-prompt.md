# Implementation Prompt

Create an original dark video collaboration or creative review interface inspired by the Frame.io Refero style.

Use a near-black cosmic background `#0a0a13` layered with dark purple/blue gradients. Use `#fcfcfc` for primary text and white pill buttons, `#6199f6` as the only blue accent, `#4f4f80` as violet product-frame border, `#757580` for secondary text, and `#a3a3b3` for tertiary text.

Use FrameGothic or a tight geometric sans substitute such as Inter, Neue Haas Grotesk, or Space Grotesk for almost everything. Set large display headings at weight 400, not bold, with tight negative tracking. Use NeueMachinaInktrap or Space Mono only for uppercase section eyebrow labels at 12px with 0.06em tracking.

Build these elements:

- Slim dark top nav with ghost links and 100px-radius pill actions.
- Full-bleed cosmic gradient hero with left-aligned display headline and stacked CTAs.
- Product UI mockup container with 10px radius, 1px Twilight border, and subtle violet glow.
- Transparent four-column feature blocks with 24px Iris Glow outline icons.
- Cinematic video showcase player with 10px radius, violet border, and minimal play overlay.
- Section eyebrow labels above major headings.

Do not copy Frame.io brand assets, exact screenshots, video stills, page copy, or proprietary layouts. Generate a new creative-production interface that borrows only the mood, token relationships, and component language.

